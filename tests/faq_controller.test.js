const request = require("supertest");

describe("tes", () => {
  test("should first", () => {
    expect(1 + 1).toEqual(2);
  });
  test("should first", () => {
    expect(1 + 1).toEqual(2);
  });
});
